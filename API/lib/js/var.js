//GLOBAL VARIABLES
var DEF_HOST 	= window.location.hostname;
var HOST 		= "http://" + DEF_HOST + "/ruangwarga/web";	//SESUAIKAN DENGAN PATH
var SEPARATOR	= "/";

var GO			= HOST + "/go.exe";

function GetHost(){
    return HOST;
}